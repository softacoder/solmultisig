[package]
name = "solmultisig"
version = "0.1.0"
description = "Created with Anchor"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "solmultisig"

# Features
[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []

# Dependencies
[dependencies]
anchor-lang = "0.31.1"
